--js-runtimes node:"C:/Program Files/nodejs/node.exe"
# Use ONE of the two cookie options below when a video says it requires cookies (uncomment only one):
# - Same machine as your Chrome browser (must run under the same Windows user account Chrome uses):
# --cookies-from-browser chrome
# - Remote/separate server (export cookies.txt from Chrome and copy it to this server):
# --cookies cookies.txt
--ffmpeg-location ffmpeg.exe